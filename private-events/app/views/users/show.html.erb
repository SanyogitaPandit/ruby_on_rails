<div class="container">
  <div class="menu-nav">
    <a><%= link_to 'All', events_path %></a>
    <a><%= link_to 'Hosted', user_path(current_user.id) %></a>
    <a><%= link_to 'Attended', attended_events_path(current_user.id) %></a>
  </div>
  <div class="clearfix">
    <h3>Upcoming Events</h3>
    <%= render 'events', locals: {givenevents: current_user.created_events.upcoming} %>      
  </div>
  <div class="clearfix">
  <br/>
    <h3>Past Events</h3>
    <%= render 'events', locals: {givenevents: current_user.created_events.past} %>      
  </div>
</div>